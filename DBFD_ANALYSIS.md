# DBFD 项目深度分析报告

## 1. 项目概览

**DBFD (Drone Based Fire Detector)** 是一个专为无人机巡检和嵌入式设备设计的火灾探测系统。该项目旨在结合深度学习的强鲁棒性和传统机器学习的高效性，构建一个实时、高精度的火灾检测解决方案。

项目包含两个主要分支：
1.  **DBFD (Desktop/Base)**: 通用桌面版本，侧重于算法研究、模型训练和数据管理。提供完整的 GUI 工具链，包括数据标注、模型自动训练和性能测试。
2.  **DBFD-Raspberry (Embedded)**: 针对树莓派 (Raspberry Pi 4B/5) 优化的嵌入式版本。侧重于低功耗、低内存占用和硬件集成（GPIO 报警、硬件编解码）。

## 2. 系统架构

项目采用模块化分层架构，确保了算法核心与应用层的解耦，便于维护和移植。

### 2.1 目录结构与职责

| 模块 | 目录 | 核心职责 |
| :--- | :--- | :--- |
| **算法层** | `algorithm/` | 封装核心检测逻辑。包括 `pnn.py` (概率神经网络)、`fusion.py` (决策融合)、`features.py` (特征提取) 和 `preprocess.py` (图像预处理)。 |
| **核心层** | `core/` | 提供系统级服务。`camera_wrapper.py` (相机抽象)、`alarm_manager.py` (硬件报警)、`memory_monitor.py` (资源监控)。 |
| **界面层** | `ui/` | 基于 PyQt6 的图形交互界面。`gui.py` (主监控台)、`data_manager_ui.py` (数据集管理工具)。 |
| **工具层** | `tools/` | 离线辅助脚本。包括 YOLO 自动训练、数据标注工具、UDP 推流测试等。 |
| **配置层** | `config.py` | (仅 Raspberry 版) 集中管理全局参数，如分辨率、内存阈值、检测间隔等。 |

### 2.2 数据流向

1.  **输入源**: 摄像头 (USB/CSI) 或 UDP 视频流 -> `CameraWrapper`
2.  **预处理**: 图像缩放、色彩空间转换 (RGB -> HSI/YCbCr) -> `preprocess.py`
3.  **检测**: 
    *   **YOLOv8**: 快速定位疑似区域 (ROI)
    *   **PNN**: 对 ROI 进行颜色/纹理特征提取与二次确认
    *   **Fusion**: 结合两者结果输出最终判定 -> `fusion.py`
4.  **输出**: 
    *   **GUI**: 实时画面渲染与边框绘制 -> `gui.py`
    *   **GPIO**: 触发蜂鸣器/LED -> `alarm_manager.py`
    *   **日志/文件**: 保存截图与事件记录 -> `output_manager.py`

## 3. 核心算法解析

### 3.1 双模态融合机制
为了平衡准确率与误报率，系统采用了串行融合策略：
*   **粗筛 (YOLOv8n)**: 利用轻量级深度学习模型快速识别画面中的"火状物体"。优点是召回率高，但可能将红色物体误报为火。
*   **精检 (PNN)**: 对 YOLO 提取的区域进行颜色直方图和 GLCM 纹理分析，输入概率神经网络进行二分类。PNN 对颜色和纹理的统计特征非常敏感，能有效剔除颜色相似但纹理不同的误报（如红色衣服、霓虹灯）。

### 3.2 近期优化 (Performance Tuning)
针对树莓派有限的计算资源，近期引入了多项关键优化：

1.  **PNN 样本压缩 (K-Means)**: 
    *   *问题*: PNN 推理速度随训练样本量线性下降，且内存占用大。
    *   *解法*: 在 `pnn.py` 中引入 K-Means 聚类，将大量原始训练样本压缩为少量的"原型向量" (Prototypes)。通过 `config.PNN_MAX_SAMPLES` 控制每类最大样本数，显著降低了内存占用和计算延迟。

2.  **跳帧检测 (Frame Skipping)**:
    *   *问题*: 逐帧检测在树莓派上会导致 FPS 过低，视频卡顿。
    *   *解法*: 在 `gui.py` 的 `AlgorithmWorker` 中实现结果缓存机制。每 `config.DETECT_INTERVAL` (如 3 帧) 执行一次完整检测，中间帧直接复用上一帧的检测结果进行绘制。大幅提升了视觉流畅度。

3.  **主动内存管理**:
    *   *问题*: Python 的垃圾回收机制在嵌入式设备上可能不够及时，导致 OOM。
    *   *解法*: `memory_monitor.py` 通过 `ctypes` 调用系统级 `malloc_trim`，在内存占用超过 `config.MAX_MEMORY_MB` 时强制释放堆内存。

## 4. 树莓派主要适配特性

*   **硬件编解码**: 摒弃了传统的 OpenCV `VideoCapture` 读取 CSI 相机的方式，改用 `libcamera-vid` 配合 UDP 传输 H.264 码流，利用 VideoCore GPU 进行硬件编码，极大减轻 CPU 负担。
*   **Headless 模式**: 支持通过 `python main.py --headless` 运行，无需连接显示器，通过 LED 灯和蜂鸣器反馈状态，适合无人机挂载场景。
*   **Systemd 服务**: 提供 `scripts/dbfd.service`，支持开机自启和崩溃自动重启。

## 5. 改进建议与路线图

1.  **算法升级**: 考虑引入轻量级 ViT (Vision Transformer) 或 MobileNet 替换 PNN 的特征提取部分，进一步提高特征表达能力。
2.  **Web 监控端**: 当前 Headless 模式缺乏可视化手段，建议开发一个轻量级 Web 服务 (基于 Flask/FastAPI)，通过浏览器查看实时视频流和报警状态。
3.  **配置热更新**: 将 `config.py` 改造为 JSON/YAML 配置文件，支持在不重启程序的情况下动态调整灵敏度或检测频率。
4.  **UDP 传输优化**: 增加应用层丢包重传或纠错机制 (FEC)，提高弱网环境下的视频传输稳定性。

## 6. 总结

DBFD 项目展示了一个完整的从算法原型到嵌入式落地的工程实践。通过合理的算法剪枝、硬件加速和软件架构优化，成功在低功耗设备上实现了可用的实时火灾检测功能。其双模态融合思路为解决复杂环境下的误报问题提供了极具价值的参考。
